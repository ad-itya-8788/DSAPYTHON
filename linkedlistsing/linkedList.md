⭐ TOP INTERVIEW QUESTIONS on Singly Linked List (PYTHON-friendly)
✅ EASY LEVEL (Must Know – 100% Asked)

Insert a node at the beginning of a linked list.

Insert a node at the end of a linked list.

Insert a node at a given position (index).

Delete the first node.

Delete the last node.

Search an element in the linked list.

Count the total number of nodes.

Reverse a linked list (iterative). ⭐ Very important

Print the middle element of a linked list.

Find the length of a linked list (iterative + recursive).

✅ MEDIUM LEVEL (Companies ask these for sure)

Reverse a linked list (recursive method).

Detect a loop in a linked list (Floyd’s Cycle Detection). ⭐

Remove loop from linked list.

Find Nth node from the end (two-pointer method). ⭐

Check if two linked lists are identical.

Merge two sorted linked lists.

Delete a node without head pointer.

Find intersection point of two linked lists.

Split linked list into 2 equal halves.

🚀 HARD LEVEL (9 LPA – 20 LPA companies love this)

Check if linked list is palindrome (O(N) time).

Sort a linked list using Merge Sort. ⭐

Rotate a linked list by k positions.

Remove duplicates from a sorted linked list.

Remove duplicates from an unsorted linked list.

Add two numbers represented by linked lists (like LeetCode). ⭐

⭐ Bonus (Most undervalued but important)

Finding cycle length in linked list.

Delete every k-th node.

Swap nodes in pairs.

Rearrange odd and even positioned nodes.

Flatten a linked list (if child pointers given).